/**
 * Shared AOI style constants.
 *
 * Centralized in one place so the same visual style is kept across uploaded
 * AOIs, committed drawn AOIs, and so on. Until design tokens are finalized,
 * these match `@mapbox/mapbox-gl-draw`'s default active theme for visual
 * consistency.
 */

export const AOI_FILL_COLOR = '#fbb03b';
export const AOI_FILL_OPACITY = 0.2;
export const AOI_LINE_COLOR = '#fbb03b';
export const AOI_LINE_WIDTH = 2;
